// Q.1 Write a JavaScript function named calculator that performs basic arithmetic operations.
// Requirements:
// The function should accept three parameters:
// num1 (number), num2 (number) and operator (string)
// Based on the value of operator, perform the following:
// 1) "+" → return the addition of num1 and num2
// 2) "-" → return the subtraction of num2 from num1
// 3) "*" → return the multiplication of num1 and num2
// 4) "/" → return the division of num1 by num2
// 5)If the operator is not valid, return "Invalid Operator".
 
// Q2. Write a function that compares two values using == and === and returns both results.
// Example: compareOperators(5, "5");
// Output :
// Loose Equality: true
// Strict Equality: false
 
// Q3. Write a function that returns the greatest of two numbers.
// Example: findMax(12, 18);
// Output : Greatest number is: 18
 
// Q4. Write a function that returns the opposite of a boolean value.
// Q6. Write a function that checks:
// 		marks ≥ 60 AND
// 		attendance ≥ 75
// 		Return "Pass" or "Fail".
// Q 7 Write a function that returns "Positive" if number > 0 else "Negative".
// Q8. What will be the output?
// const obj = {
//   name: "Jyoti",
//   greet: function () {
//     console.log(this.name);
  
// Q9. Write a function using rest operator that:
// Accepts unlimited numbers
// Returns their sum
// Example:
// sum(1,2,3,4)
// Q10. Use spread operator to pass array values as function arguments.
// Example: const arr = [5, 10, 15];